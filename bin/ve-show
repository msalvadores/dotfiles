#!/bin/zsh

green='\e[0;32m'
blue='\e[0;34m'
red='\e[0;31m'
NC='\e[0m' # No Color

if [ -z "$VIRTUAL_ENV" ]; then
    echo -e "${red}Virtual environment is not set${NC}"
else
    echo -e "Virtual environment set to:"
    echo -e "   ${green}${VIRTUAL_ENV}${NC}"
fi
